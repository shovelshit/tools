# 通用工作约束

- 以当前代码、配置和实时数据核实范围；历史文档与少量样本不能代替现状检查。保留工作区中与任务无关的改动。
- 迁移前区分可舍弃的历史与必须保留的当前状态、配置、进行中的任务和待处理消息；清点所有受影响的角色与数据类别。
- 生产切换先备份、记录基线并真正暂停相关写入和调度；设计可重试的幂等迁移，不覆盖已有进度，也不凭“已暂停”参数推断系统已停。
- 远程写入后独立核对数量、版本和任务状态；客户端报错时先查实际结果再重试。恢复调度后观察完整运行，确认稳定前保留回滚材料。
- 比较性能时固定工作负载，分别报告读、写和数据增长，明确标注实测、推算或目标值；不要把减少持久化写入说成零写入。
- 测试覆盖正常、失败和重试；生产效果仍需现场验证。区分本地缺少环境变量与远程未配置密钥，只核对密钥名称，不输出密钥值或提交备份。
