# 统一按 LF 存库与检出: 避免 Windows(core.autocrlf=true) 把源码检出成 CRLF,
# 导致依赖换行的前端测试(pages/maoyan/app.test.cjs)必然失败。
* text=auto eol=lf

# 二进制文件不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.woff binary
*.woff2 binary
